{
  "name": "smart-form-validator",
  "version": "1.0.0",
  "description": "AI-powered Smart Form Validator with React frontend and Node.js backend",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["form", "validator", "ai", "security"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.2",
    "validator": "^13.11.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
